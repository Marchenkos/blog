<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  </head>

  <body class="relative pb-16 bg-[#f5f5f5]" data-controller="layout" data-layout-hide-class="animate-fadeOut" data-layout-show-class="animate-fadeIn">
    <%= render partial: 'shared/navbar' %>

    <main data-layout-target="mainContent" class="w-screen min-h-screen">
      <%= yield %>
    </main>
    <span data-layout-target='cursorCircle' class="absolute z-10 border border-[#ccc] w-[30px] h-[30px] rounded-full pointer-events-none"></span>

    <div data-controller="turbo-modal" data-action="turbo:frame-load->turbo-modal#open turbo:submit-end->turbo-modal#close" class="app-modal-window hidden z-10 absolute inset-0 bg-color-util-shade-20">
      <div class="fixed inset-0 bg-color-util-shade-40 flex justify-center items-center"></div>
        <%= turbo_frame_tag "modal", data: { turbo_modal_target: 'content' } %>
      </div>
    </div>
  </body>
</html>
